#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);//取消与stdio同步
    //if(!cin) cout<<"error"<<endl;
    //char a=cin.get();
    //cin.get(a);
    char a[205];
    gets(a);//一行
    char a;
    a=getchar();//读取一个变量赋值给a，没有空格也是读取一个字符
    //cin.get(ch,20,'/');cin.getline(ch,20,'/');
    string s;
    getline(cin,s);
    //cout.put('a').put(' ');
    putchar(a);
    return 0;
}
//异常处理可以用输出中间结果来代替
